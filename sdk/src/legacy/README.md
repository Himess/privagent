# Legacy V3 Code (Deprecated)

These files are from the V3 single-note privacy model. They are kept for backward compatibility but should not be used for new development.

**Use the V4 API instead:**
- `ShieldedWallet` (from `v4/shieldedWallet.ts`) replaces `ShieldedPoolClient`
- `createUTXO` replaces `createNote`
- `ghostPaywallV4` / `ghostFetchV4` replace `ghostPaywall` / `ghostFetch`
